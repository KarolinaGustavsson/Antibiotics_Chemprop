# Chemprop documentation: https://chemprop.readthedocs.io/en/latest/

## Training (for article dataset)
chemprop_train --data_path <path> --dataset_type <type> --save_dir <dir>

## hyperparameter optimization (for coco, n=20)
chemprop_hyperopt --data_path <data_path> --dataset_type <type> --num_iters <n> --config_save_path <config_path>

## Predicting
chemprop_predict --test_path <test_path> --checkpoint_dir <checkpoints> --preds_path <preds>

## Interpreting (model interprability - which chemical groups give these properties?)
chemprop_interpret --data_path <path> --checkpoint_dir <dir> --property_id 1

